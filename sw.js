// Un service worker muy básico para cumplir el requisito de PWA.
// No hace nada más que instalarse.
self.addEventListener("install", (event) => {
  console.log("Service Worker instalado");
});
